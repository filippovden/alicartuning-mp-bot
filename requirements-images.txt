# Опциональные тяжёлые зависимости для удаления фона (раздел 11 ТЗ, V2).
# Устанавливаются отдельно: pip install -r requirements-images.txt
# При первом запуске rembg скачивает ONNX-модель U^2-Net (~176 МБ) — нужен доступ в интернет.
-r requirements.txt
rembg>=2.0.59
onnxruntime>=1.19
